# UI QA Harness

앞으로 UI 수정은 아래 순서를 통과한 뒤에만 `완료`로 본다.

1. **기대값 정의**
   - 바꾸려는 위치/간격/표기/동작을 먼저 짧게 확정한다.
2. **구현**
   - 가능한 한 기존 구조를 유지하고 최소 범위만 수정한다.
3. **소스 재확인**
   - 커밋 직후 GitHub에서 수정 파일을 다시 읽어 기대한 코드가 실제로 저장됐는지 확인한다.
4. **UI Smoke Test**
   - 관련 QA fixture를 브라우저에서 렌더링한다.
   - 헤더 변경 시 `qa/header-smoke.html`의 `data-qa=PASS`를 확인한다.
   - 현재 체크 항목: D-Day 표시, 하이픈 폭/수평, D-Day↔날짜 간격, 날짜↔장소 간격, 장소↔주소 간격, 복사 버튼 크기.
5. **배포 상태 확인**
   - 해당 커밋의 GitHub Pages 배포가 완료됐는지 확인한다.
6. **최종 보고**
   - 3~5단계를 통과한 경우에만 사용자에게 `적용 완료`라고 말한다.
   - 실패하면 사용자 확인을 요청하기 전에 먼저 원인을 수정하고 다시 검수한다.

## 원칙

- `커밋 성공`과 `화면 적용 성공`을 같은 의미로 취급하지 않는다.
- 시각 수정은 가능하면 숫자 기준(폭, 높이, 간격)을 둔다.
- 캐시 영향을 받는 CSS/JS 변경은 버전 쿼리 또는 새 파일명으로 명확히 갱신한다.
- 사용자가 스크린샷으로 문제를 지적하면 그 스크린샷을 회귀 테스트 기준으로 추가한다.
